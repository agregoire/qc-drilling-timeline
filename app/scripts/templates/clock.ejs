<span id="year"><%= year %></span>
<button name="play" id="play"
 class="<% if (running) { %>running<% } else { %>stopped<% } %>">
  <% if (running) { %>
  Arrêter
  <% } else { %>
  Démarrer
  <% } %>
</button>